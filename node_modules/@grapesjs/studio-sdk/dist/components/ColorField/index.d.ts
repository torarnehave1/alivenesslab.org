/// <reference types="react" />
export interface Props {
    className?: string;
    value: string;
    placeholder?: string;
    label?: React.ReactNode;
    size?: 'm' | 's';
    preventSubmitOnEnter?: boolean;
    onChange?(value: string | null, partial?: boolean): void;
}
export default function ColorField({ className, value, label, size, placeholder, preventSubmitOnEnter, onChange }: Props): import("react/jsx-runtime").JSX.Element;
