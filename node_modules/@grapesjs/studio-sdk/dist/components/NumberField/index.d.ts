/// <reference types="react" />
import { LabelFieldProps } from '../LabelField';
import { MinMaxStepProps } from './utils';
export interface NumberFieldProps extends MinMaxStepProps {
    className?: string;
    value: string | number;
    valueUnit?: string;
    units?: string[];
    placeholder?: string;
    pan?: React.ReactNode | boolean;
    label?: React.ReactNode;
    size?: 'm' | 's';
    tip?: LabelFieldProps['tip'];
    onChange?(value: string | number | null, partial?: boolean): void;
    onChangeUnit?(value: string | null): void;
}
export default function NumberField({ className, value, units, valueUnit, label, size, pan, min, max, step, tip, placeholder, onChange, onChangeUnit }: NumberFieldProps): import("react/jsx-runtime").JSX.Element;
