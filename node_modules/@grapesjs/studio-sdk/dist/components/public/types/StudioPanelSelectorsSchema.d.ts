import { StudioLayouts } from '.';
import type { StudioLayoutPanelConfig } from './StudioPanelSchema';
export interface StudioLayoutSelectorManagerConfig {
    /**
     * Enable the style catalog.
     * @default false
     */
    styleCatalog?: boolean;
    /**
     * Enable the state selector.
     * @default false
     */
    stateSelector?: boolean;
}
export interface StudioLayoutPanelSelectorsConfig extends StudioLayoutPanelConfig, StudioLayoutSelectorManagerConfig {
    /**
     * Type of the layout component.
     */
    type: `${StudioLayouts.panelSelectors}`;
}
