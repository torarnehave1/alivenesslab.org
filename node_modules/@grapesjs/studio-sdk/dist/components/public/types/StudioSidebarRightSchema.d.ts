import { StudioLayouts } from '.';
import type { StudioLayoutBasePropsConfig } from './StudioBaseSchema';
export interface StudioLayoutSidebarRightConfig extends Omit<StudioLayoutBasePropsConfig, 'htmlAttrs'> {
    /**
     * Type of the layout component.
     */
    type: `${StudioLayouts.sidebarRight}`;
}
