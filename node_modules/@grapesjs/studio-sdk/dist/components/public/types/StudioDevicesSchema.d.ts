import { StudioLayouts } from '.';
import type { StudioLayoutBasePropsConfig } from './StudioBaseSchema';
export interface StudioLayoutDevicesConfig extends Omit<StudioLayoutBasePropsConfig, 'children' | 'htmlAttrs'> {
    /**
     * Type of the layout component.
     */
    type: `${StudioLayouts.devices}`;
}
