import { StudioLayouts } from '.';
import type { StudioLayoutFlexConfig } from './StudioBaseSchema';
export interface StudioLayoutCanvasConfig extends Pick<StudioLayoutFlexConfig, 'id' | 'className' | 'style' | 'grow'> {
    /**
     * Type of the layout component.
     */
    type: `${StudioLayouts.canvas}`;
}
