import { StudioLayouts } from '.';
import type { StudioLayoutPanelConfig } from './StudioPanelSchema';
export interface StudioLayoutPanelPageSettingsConfig extends StudioLayoutPanelConfig {
    /**
     * Type of the layout component.
     */
    type: `${StudioLayouts.panelPageSettings}`;
    /**
     * The page to display settings for.
     * @examples editor.Pages.getSelected();
     */
    page?: 'Page';
}
