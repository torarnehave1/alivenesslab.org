/// <reference types="react" />
import { ResizableProps } from 're-resizable';
export interface Props {
    children: React.ReactNode;
    className?: string;
    bottom?: boolean;
    top?: boolean;
    right?: boolean;
    left?: boolean;
    width?: string | number;
    height?: string | number;
    minHeight?: string | number;
    minWidth?: string | number;
    maxHeight?: string | number;
    maxWidth?: string | number;
    enable?: ResizableProps['enable'];
    style?: ResizableProps['style'];
    onResize?: ResizableProps['onResize'];
    onResizeStop?: ResizableProps['onResizeStop'];
}
export default function Resize({ className, children, top, bottom, right, left, width, height, minHeight, minWidth, maxHeight, maxWidth, style, onResize, onResizeStop, ...rest }: Props): import("react/jsx-runtime").JSX.Element;
