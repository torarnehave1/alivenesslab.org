/// <reference types="react" />
import { LabelFieldProps } from '../LabelField';
export interface SwitchFieldProps {
    value: boolean;
    border?: boolean;
    placeholder?: string;
    tip?: LabelFieldProps['tip'];
    label?: React.ReactNode;
    size?: 'm' | 's';
    disabled?: boolean;
    onChange(checked: boolean): void;
}
export default function SwitchField({ value, label, border, size, tip, disabled, onChange }: SwitchFieldProps): import("react/jsx-runtime").JSX.Element;
