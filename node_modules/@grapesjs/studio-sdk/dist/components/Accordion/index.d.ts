/// <reference types="react" />
import { StudioIconProps } from '../public/StudioIcon';
export interface AccordionProps {
    children?: React.ReactNode;
    style?: React.CSSProperties;
    className?: string | ((open: boolean) => string);
    handler?: React.ReactNode | ((open: boolean) => React.ReactNode);
    handlerIcon?: StudioIconProps['icon'];
    open?: boolean;
    inverted?: boolean;
    disabled?: boolean;
    sticky?: boolean;
    classNameWrap?: string;
    classNameContent?: string;
    onChange?: (props: {
        event: Event;
        open: boolean;
    }) => void;
}
export default function Accordion({ className, classNameWrap, classNameContent, style, handler, handlerIcon, children, inverted, disabled, sticky, onChange, open: isOpen }: AccordionProps): import("react/jsx-runtime").JSX.Element;
