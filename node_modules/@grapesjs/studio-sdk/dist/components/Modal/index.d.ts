/// <reference types="react" />
import { ButtonProps } from '../Button';
export declare interface ModalProps {
    children: React.ReactNode;
    className?: string;
    open?: boolean;
    title?: React.ReactNode;
    description?: React.ReactNode;
    buttons?: ButtonProps[];
    fullH?: boolean;
    closable?: boolean;
    disablePropagation?: boolean;
    isDarkTheme?: boolean;
    size?: 'lg' | 'l' | 'm' | 's';
    onClose(value?: boolean): void;
}
export default function Modal({ open, title, disablePropagation, isDarkTheme, description, children, onClose, closable, fullH, buttons, size }: ModalProps): import("react/jsx-runtime").JSX.Element;
