import { ReactNode } from 'react';
import { ButtonProps } from '../../Button';
export interface ImportCodeProps {
    buttonProps?: ButtonProps;
    content?: ReactNode;
    code?: string;
    onAction?(code: string): void;
}
declare const _default: import("react").FunctionComponent<ImportCodeProps>;
export default _default;
