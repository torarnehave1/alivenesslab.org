/// <reference types="react" />
import { ButtonWithTooltipProps } from '../Button/ButtonWithTooltip';
import { GridProps } from '../Grid';
export interface CardTitleButtons extends ButtonWithTooltipProps {
    id: string;
    icon?: string;
}
export interface CardTitleProps extends Omit<GridProps, 'title'> {
    title?: React.ReactNode;
    icon?: string;
    className?: string;
    onClose?(): void;
    actions?: CardTitleButtons[];
}
export default function CardTitle({ className, icon, title, onClose, actions, children }: CardTitleProps): import("react/jsx-runtime").JSX.Element;
