/// <reference types="react" />
export interface StackFieldProps<T> {
    className?: string;
    items: T[];
    selected: T | null;
    renderItem: (item: T) => React.ReactNode;
    select?: (item: T | null) => void;
    add?: () => void;
    remove?: (item: T) => void;
    move?: (item: T, index: number) => void;
    reset?: (items: T[]) => void;
    previewStyle?: (items: T) => Record<string, string>;
    label?: React.ReactNode;
    children?: React.ReactNode;
    title?: React.ReactNode;
    titleAdd?: React.ReactNode;
}
export default function StackField<T = any>({ className, items, selected, children, title, titleAdd, label, select, add, move, remove, renderItem, previewStyle }: StackFieldProps<T>): import("react/jsx-runtime").JSX.Element;
