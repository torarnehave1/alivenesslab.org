/// <reference types="react" />
export type Props = React.HTMLProps<HTMLDivElement> & {
    label?: React.ReactNode;
    pill?: boolean;
    border?: boolean;
    block?: boolean;
    nopad?: boolean;
    s?: 'xs' | 's' | 'm';
};
export default function Badge({ children, label, s, pill, border, nopad, className, block, ...props }: Props): import("react/jsx-runtime").JSX.Element;
